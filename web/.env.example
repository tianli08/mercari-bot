# Browser-facing API prefix. Use localhost (not 127.0.0.1) so the request
# origin matches the backend CORS allowlist and cookies attach to one host.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
